analyzes gun murders as reported by FBI

download-data.R   downloads data as a CSV file in data directory

wrangle-data.R  creates a derived dataset and saves as R object in rda directory.

analysis.R  reorders data by states with highest murders and saves bar graph in figs directory.